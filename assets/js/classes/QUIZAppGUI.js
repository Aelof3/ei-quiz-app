export default class QUIZAppGUI {
    constructor( data ){
        this.init( );
    }

    init( ){
        this.buildMenu( );
        this.loop( );
        this.onCreate( );
    }

    buildMenu( ){
        // build the menu HTML
    }

    toggleMenu( ){
        // toggle menu
    }
    
    onCreate( ){
        // on create hook
    }

    loop( ){
        // run in main loop
    }
}